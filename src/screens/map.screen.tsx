import React, { ReactElement } from 'react'
import {
   Text,
   View
} from 'react-native'

const MapScreen: React.FC = (): ReactElement => {
   return (
      <View>
         <Text>MapScreen</Text>
      </View>
   )
}

export default MapScreen
